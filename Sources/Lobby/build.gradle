/**
 * Lobby build script
 */

dependencies {
    shadow project(path: ':Sources:API', configuration: 'shadow')
    shadow project(path: ':Sources:GlobalShops', configuration: 'shadow')
    shadow project(path: ':Sources:NoPremiumAuth', configuration: 'shadow')
}